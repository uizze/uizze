name: UIZZE UI review

on:
  pull_request:
    paths:
      - "app/**"
      - "components/**"
      - ".uizze/**"
      - ".github/uizze-ui-evidence.json"
  workflow_dispatch:
  workflow_call:

permissions:
  contents: read

jobs:
  finish-gate:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 2
      - uses: uizze/uizze@v1
        with:
          manifest: .github/uizze-ui-evidence.json
          fail-on: error
          show-uizze-link: "true"
