# Security

Report vulnerabilities through GitHub private vulnerability reporting for this
repository. Do not open a public issue for a vulnerability.

The starter and bundled free skill need no credentials. The optional MCP setup
reads a token from your environment; never commit `.env` files, tokens, client
configuration containing a token, or screenshots that expose secrets.
