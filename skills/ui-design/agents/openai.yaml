interface:
  display_name: "Uizze UI Design"
  short_description: "Product-specific UI, craft checks and Overdrive"
  default_prompt: "Use $ui-design to build this UI from a clear design direction, relevant references, and visual verification."
